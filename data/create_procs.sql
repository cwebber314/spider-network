CREATE PROC GetNetworkEdges(@networkid int, @nodeid int) 
AS 
BEGIN
-- Retrieve the network edges in the neighborhood of the provided node
END

CREATE PROC GetNetworkNodes(@networkid int, @nodeid int) 
AS 
BEGIN
-- Retrieve the network nodes in the neighborhood of the provided node
END

CREATE PROC ListCases() 
AS 
BEGIN
-- List all cases/scenarios.  
END

CREATE PROC GetNodes(@networkid) 
AS 
BEGIN
-- List all nodes in the case.  
-- Use this to build a node select UI
END
